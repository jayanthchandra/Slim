---
name: slim
description: "Manage MCP tool signatures (init, status, update, inspect, scrub)."
---

Execute the following shell command and return its output:
!`slim $ARGUMENTS`

If arguments are not provided, show the available subcommands: init, status, update, inspect, scrub.
